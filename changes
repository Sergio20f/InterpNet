Changes:
 * model_setup.py
    - build_and_compile -> model = None removed
    - step_train -> DELETED
    - training_fit_loop -> Added epochs param, delete everything related to fitting
    
    
For now, stick to custom_dir = False (it won't work otherwise atm)

 * data_loading.py
    - deleted the variable dts
